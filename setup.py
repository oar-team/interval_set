from setuptools import setup

setup(name='interval_set',
      version='1.1',
      description=(
        'Closed interval set toolkit for resources representation in '
        'scheduling'),
      url='https://github.com/oar-team/interval_set',
      author='Michael Mercier',
      author_email='michael.mercier@inria.fr',
      license='BSD',
      packages=['interval_set'],
      zip_safe=False)
